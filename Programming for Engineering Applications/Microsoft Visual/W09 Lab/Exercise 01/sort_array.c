#include <stdio.h>
#include <stdlib.h>

void sort_array(int* pointer_array, int array_size)
{
	printf("Sorting array.");

	for (int i = 0; i < array_size; i++)
	{
		pointer_array[i] = 1;
	}

	printf("\n\n");
}